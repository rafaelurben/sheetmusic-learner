name: CI

on:
  push:
    branches:
      - "**"
  workflow_dispatch:

env:
  CI_REGISTRY_IMAGE_BASE: ${{ vars.CI_REGISTRY_IMAGE_BASE }}

permissions:
  contents: read

jobs:
  checks-backend:
    name: checks::backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
      - name: Run backend formatting check
        run: mvn -B -f backend/pom.xml spotless:check

  checks-backend-tests:
    name: checks::backend-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
      - name: Run backend tests
        run: mvn -B -f backend/pom.xml test
      - name: Upload backend test reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: backend-surefire-reports
          path: backend/target/surefire-reports/
          if-no-files-found: warn

  checks-frontend-tests:
    name: checks::frontend-tests
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/rafaelurben/node25-trixie-with-jdk25:latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Cache npm data
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: frontend/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-
      - name: Run frontend tests
        run: |
          cd frontend
          npm ci --cache .npm --prefer-offline
          npm run generate:openapi
          npm run test:ci
      - name: Upload frontend test and coverage reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: frontend-test-results
          path: |
            frontend/test-results/
            frontend/coverage/
          if-no-files-found: warn

  checks-frontend:
    name: checks::frontend
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/rafaelurben/node25-trixie-with-jdk25:latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Cache npm data
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: frontend/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-
      - name: Run frontend checks
        run: |
          cd frontend
          npm ci --cache .npm --prefer-offline
          npm run generate:openapi
          npm run lint
          npm run typescript-check

  build-backend:
    name: build::backend
    runs-on: ubuntu-latest
    needs:
      - checks-backend
      - checks-backend-tests
      - checks-frontend
      - checks-frontend-tests
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
      - name: Build backend package
        run: mvn -B -f backend/pom.xml -DskipTests package
      - name: Upload backend build artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: backend-jar
          path: backend/target/backend-*.jar
          if-no-files-found: error

  build-frontend:
    name: build::frontend
    runs-on: ubuntu-latest
    needs:
      - checks-backend
      - checks-backend-tests
      - checks-frontend
      - checks-frontend-tests
    container:
      image: ghcr.io/rafaelurben/node25-trixie-with-jdk25:latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Cache npm data
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: frontend/.npm
          key: npm-${{ runner.os }}-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            npm-${{ runner.os }}-
      - name: Build frontend
        run: |
          cd frontend
          npm ci --cache .npm --prefer-offline
          npm run build
      - name: Upload frontend build artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: frontend-build
          path: |
            frontend/dist
            frontend/nginx.conf
          if-no-files-found: error

  docker-backend:
    name: docker::backend
    runs-on: ubuntu-latest
    needs:
      - build-backend
    if: github.ref_name == 'main' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Download backend artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: backend-jar
          path: backend/target
      - name: Prepare backend image tags
        id: backend-tags
        run: |
          IMAGE="${CI_REGISTRY_IMAGE_BASE}/sheetmusic-learner/sheetmusic-learner-backend"
          echo "sha_tag=${IMAGE}:${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            echo "latest_tag=${IMAGE}:latest" >> "$GITHUB_OUTPUT"
          fi
      - name: Login to registry
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ${{ vars.CI_REGISTRY }}
          username: ${{ vars.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_PASSWORD }}
      - name: Build and push backend image
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7.2.0
        with:
          context: .
          file: backend/Dockerfile
          push: true
          tags: |
            ${{ steps.backend-tags.outputs.sha_tag }}
            ${{ steps.backend-tags.outputs.latest_tag }}

  docker-frontend:
    name: docker::frontend
    runs-on: ubuntu-latest
    needs:
      - build-frontend
    if: github.ref_name == 'main' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Download frontend artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: frontend-build
          path: frontend
      - name: Prepare frontend image tags
        id: frontend-tags
        run: |
          IMAGE="${CI_REGISTRY_IMAGE_BASE}/sheetmusic-learner/sheetmusic-learner-frontend"
          echo "sha_tag=${IMAGE}:${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            echo "latest_tag=${IMAGE}:latest" >> "$GITHUB_OUTPUT"
          fi
      - name: Login to registry
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ${{ vars.CI_REGISTRY }}
          username: ${{ vars.CI_REGISTRY_USER }}
          password: ${{ secrets.CI_REGISTRY_PASSWORD }}
      - name: Build and push frontend image
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7.2.0
        with:
          context: .
          file: frontend/Dockerfile
          push: true
          tags: |
            ${{ steps.frontend-tags.outputs.sha_tag }}
            ${{ steps.frontend-tags.outputs.latest_tag }}
